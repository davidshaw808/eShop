using System;
namespace Common.Enum
{
    public enum Currency
    {
        GBP
    }

    public enum PaymentProvider
    {
        Paypal
    }

    public enum OrderStatus
    {
        Paid,
        Shipped,
        InTransit,
        Delivered
    }
}
